# .NET
bin/
obj/
*.user
*.suo
.vs/
.vscode/*.log

# Packages restored by legacy NuGet (packages.config)
src/ContosoInsurance/packages/

# azd / infra state
.azure/
azure.env

# generated by docs/scan-progress.ps1 (repo-scan output consumed by docs/tracker.html)
docs/tracker-status.js

# Node (in case anyone adds a Blazor/JS asset pipeline)
node_modules/

# BMAD METHOD tooling — regenerated locally via the BMAD installer
# (`npx bmad-method install`). Do NOT commit; teammates reinstall after pulling.
.agents/
.claude/
.github/agents/
_bmad/
_bmad-output/

# OS
Thumbs.db
.DS_Store

# Local secrets (guardrail — do NOT commit)
appsettings.Development.json
appsettings.Local.json
*.pfx
*.key
